{
  "name": "claude-companion",
  "private": true,
  "version": "0.1.0",
  "description": "Token/cache cockpit for Claude Code: cost visibility, cache-TTL countdown, keep-warm, advisor, usage-limit guardian",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "daemon": "node packages/daemon/src/index.ts",
    "doctor": "node packages/cli/src/ccc.ts doctor"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "happy-dom": "^20.10.6",
    "typescript": "^5.8.0",
    "vitest": "^3.2.7"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
